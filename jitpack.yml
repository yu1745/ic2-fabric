# JitPack 构建配置
# Fabric Loom 1.15+ 强制要求 JVM 21，字节码 target 仍为 17 (release=17)
jdk:
  - openjdk21

# 只发布 core 子项目（带 access widener + API），addon 子项目不需要发布给下游
install:
  - >
    ./gradlew :core:publishToMavenLocal
    -Pgroup=$GROUP
    --no-daemon
    -x :core:test
    -x :core:datagen
